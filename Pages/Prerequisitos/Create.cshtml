@page
@model ProyectoBaseDD.Pages.Prerequisitos.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Prerequisito</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Prerequisito.IdPrerequisitos" class="control-label"></label>
                <input asp-for="Prerequisito.IdPrerequisitos" class="form-control" />
                <span asp-validation-for="Prerequisito.IdPrerequisitos" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Prerequisito.CveAsig" class="control-label"></label>
                <select asp-for="Prerequisito.CveAsig" class ="form-control" asp-items="ViewBag.CveAsig"></select>
            </div>
            <div class="form-group">
                <label asp-for="Prerequisito.IdKardex" class="control-label"></label>
                <select asp-for="Prerequisito.IdKardex" class ="form-control" asp-items="ViewBag.IdKardex"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
